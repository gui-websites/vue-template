{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "DOCKER_HOST=\"ssh://SSH_HOST\" docker-compose down; DOCKER_HOST=\"ssh://SSH_HOST\" docker-compose up -d",
    "compose": "docker-compose down; docker-compose up -d"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^5.3.2",
    "nuxt": "3.0.0-rc.9",
    "tailwindcss": "^3.1.8"
  },
  "dependencies": {
    "@vueuse/nuxt": "^9.3.1",
    "dayjs": "^1.11.5",
    "feather-icons": "^4.29.0",
    "pocketbase": "^0.7.4",
    "prettier-plugin-tailwindcss": "^0.1.13",
    "vue-feather": "^2.0.0"
  }
}
